<table id="listarCortes" class="table table-striped" style="width:100%">
  <thead>
    <tr>
      <th>Número</th>
      <th>Proveedor</th>
      <th>Cantidad</th>
      <th>Medida</th>
      <th>Estado de corte</th>
    </tr>
  </thead>
  <tbody>
    {% for corte in cortes %}
      <tr>
        <td>{{ corte.numero }}</td>
        <td>{{ corte.proveedor }}</td>
        <td>{{ corte.cantidad }}</td>
        <td>{{ corte.medida }}</td>
        <td>
          <select class="form-control estadoCorte" data-corte-id="{{ corte.numero }}">
            {% for estado in estados %}
              <option value="{{ estado }}">{{ estado }}</option>
            {% endfor %}
          </select>
        </td>
      </tr>
    {% endfor %}
  </tbody>
</table>
<script src="/js/cortesproducto/listarcortes.js"></script>

